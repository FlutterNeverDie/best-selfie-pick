import 'package:flutter/material.dart';

class MyContestScreen extends StatelessWidget {
  const MyContestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text('MyContestScreen')],
      ),
    );
  }
}
