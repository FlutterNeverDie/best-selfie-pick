

import 'package:flutter/material.dart';

// 고정 값을 저장해놓고 값의 변동이 없는 것들만 클래스로 만들자
class AppColor {
  AppColor._();
  static const primary = Colors.pinkAccent;
  static const scaffoldBackgroundColor = Color(0xFFF8F9FB);
  static const appBarBackgroundColor = Color(0xFCF8F9FB);
  static const bottomNavigationBackgroundColor = Color(0xFCF8F9FB);
  static const boldHintText = Color.fromARGB(255, 129, 128, 138);
  static const hintText = Color.fromARGB(255, 155, 154, 159);
  static const hintText2 = Color(0xffACACAC);

  // 라임 강조색
  static const lime = Color(0xffA0D700);

  // 라임 기본 색
  static const originLime = Color(0xffBFFF05);

  // 디자이너 라임색
  static const parkLime = Color(0xffDAFF61);


  static const mainList = Color.fromARGB(255, 0, 70, 111);
  static const mediumBlue = Color.fromARGB(255, 60, 140, 180);
  static const darkOrange = Color.fromARGB(255, 222, 112, 48);
  static const faleBlue = Color.fromARGB(255, 44, 44, 44);
  static const brightBlue = Color(0xFFDDE5F6);
  static const salmon = Color(0xffff6666);


  // black
  static const black = Color(0xFF000000);

  // white
  static const white = Color(0xFFFFFFFF);
  static const safeBackground = Color(0xFFFFFFFF);

}